/** The whale rendered with the palette (each sprite cell mapped once). */
export declare function whaleMarked(): string[];
/** Whale rows padded to a fixed column width (for side-by-side wordmark layout). */
export declare function whaleMarkedPadded(width?: number): string[];
/** Marked width in columns (for layout math). */
export declare const WHALE_WIDTH = 24;
export declare const WHALE_HEIGHT: number;
//# sourceMappingURL=logo.d.ts.map